{
  "mcpServers": {
    "postgres": {
      "command": "bash",
      "args": ["-c", "npx -y @modelcontextprotocol/server-postgres postgresql://$DB_USER:$DB_PASSWORD@$DB_HOST:$DB_PORT/$DB_NAME"]
    }
  }
}
