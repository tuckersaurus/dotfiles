{
  "name": "@{{cookiecutter.project_owner}}/{{cookiecutter.package}}",
  "version": "0.0.0",
  "private": true,
  "workspaces": [],
  "devDependencies": {
    "@tailwindcss/cli": "^4.0.0",
    "cpx2": "^7.0.0",
    "esbuild": "^0.27.2",
    "eslint": "^8.0.0",
    "postcss-scss": "^4.0.9",
    "prettier": "^3.0.0",
    "sass": "^1.97.2",
    "stylelint": "^17.0.0",
    "stylelint-config-standard-scss": "^17.0.0",
    "stylelint-scss": "^7.0.0",
    "tailwindcss": "^4.0.0",
    "typescript": "^5.9.3"
  },
  "scripts": {
    "-- EXAMPLE: Workspace Script --": "npm --workspace=@{{cookiecutter.project_owner}}/<project-package-name> run <command>",
    "-- LINTING --": "Commands for linting JavaScript/TypeScript and CSS/SCSS files.",
    "lint": "echo \"Add project lint scripts here.\"",
    "-- TYPE CHECKING --": "Commands for TypeScript type checking.",
    "typecheck": "echo \"Add project typecheck scripts here.\"",
    "-- FORMATTING --": "Commands for formatting JavaScript/TypeScript and CSS/SCSS files.",
    "format": "echo \"Run prettier in your project or workspaces.\"",
    "-- BUILDING --": "Commands for building CSS and JS files for various projects.",
    "build": "echo \"Add project build scripts here.\"",
    "-- BOOTSTRAP --": "Command to install all dependencies for the project.",
    "bootstrap": "npm ci",
    "-- WEB --": "Web-specific scripts exposed for workspace coordination.",
    "watch:web": "echo \"Replace with Tailwind watch, e.g: ../../../node_modules/.bin/tailwindcss -i ./wwwroot/css/app.css -o ./wwwroot/css/app.output.css --watch\""
  }
}
